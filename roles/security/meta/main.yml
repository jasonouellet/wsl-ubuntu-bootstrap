---
# =========================================
# Security Role Dependencies
# =========================================
dependencies:
  - role: python
    when: security_enable | default(true)

# Role metadata
galaxy_info:
  author: WSL Bootstrap Contributors
  description: Security scanning tools (Trivy, Syft, detect-secrets)
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
    - name: Debian
      versions:
        - bullseye
        - bookworm
  galaxy_tags:
    - security
    - scanning
    - trivy
    - syft
    - secrets
